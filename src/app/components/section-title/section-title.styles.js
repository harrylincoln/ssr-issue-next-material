const productSelectorStyles = (theme) => ({
    section: {},
    title: {
        ...theme.pallete.primary.typography.sectionTitle,
    },
    subtitle: {
        ...theme.pallete.primary.typography.sectionSubTitle,
    },
});

export default productSelectorStyles;
